export const sendMessage = (_req, res) => {
  res.status(501).json({ message: "Not implemented in scaffold mode" });
};

export const getMessagesBetweenUsers = (_req, res) => {
  res.status(501).json({ message: "Not implemented in scaffold mode" });
};
